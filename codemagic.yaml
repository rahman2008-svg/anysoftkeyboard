workflows:
  android-build:
    name: AnySoftKeyboard Build

    environment:
      java: 21

    scripts:
      - name: Grant permissions
        script: chmod +x gradlew

      - name: Build APK
        script: ./gradlew :ime:app:assembleDebug

    artifacts:
      - ime/app/build/outputs/**/*.apk
